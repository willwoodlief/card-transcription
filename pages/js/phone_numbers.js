$(function() {
    $('#phone').formatter({
        'pattern': '({{999}}) {{999}}.{{9999}}',
        'persistent': true
    });

    $('#cell_phone').formatter({
        'pattern': '({{999}}) {{999}}.{{9999}}',
        'persistent': true
    });

    $('#fax').formatter({
        'pattern': '({{999}}) {{999}}.{{9999}}',
        'persistent': true
    });


});